#include "main.h"

/**
 * _abs - returns the value of an absolute value of an integer
 * @n: the int to check 
 * Return: absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	r = _abs (-1)
	{
	return (n);
	}
	return (-n);
}
