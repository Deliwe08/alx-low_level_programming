#include "main.h"


/**
 * _isupper - check if letter c is uppercase
 * @c: the number to be checked
 *
 * Returns: 1 or 0
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
